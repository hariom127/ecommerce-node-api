import React from "react";
import Layout from "../../components/Layout";

const Orders = (props) => {
  return (
    <Layout sidebar>
      <p>orders</p>
    </Layout>
  );
};

export default Orders;
