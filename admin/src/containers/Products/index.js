import React from "react";
import Layout from "../../components/Layout";

const Product = (props) => {
  return (
    <Layout sidebar>
      <p>Products</p>
    </Layout>
  );
};
export default Product;
